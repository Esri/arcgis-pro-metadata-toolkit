<p:EditorPage x:Class="ArcGIS.Desktop.Metadata.Editor.Pages.MD_ClassificationCode" x:ClassModifier="internal" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:p="clr-namespace:ArcGIS.Desktop.Metadata.Editor.Pages"
             xmlns:v="clr-namespace:ArcGIS.Desktop.Metadata.Editor.Validation"
             xmlns:e="clr-namespace:ArcGIS.Desktop.Metadata.Editor"
             xmlns:r="clr-namespace:ArcGIS.Desktop.Internal.Metadata.Properties" 
             xmlns:converters="clr-namespace:ArcGIS.Desktop.Metadata.Pages.Converters"
             mc:Ignorable="d" >
  <UserControl.Resources>
    <converters:ListBoxItemToAutomationIdConverter x:Key="_lbiToAutoIdConverter" />  
    <e:Codelists x:Key="MD_ClassificationCode" CodelistName="MD_ClassificationCode"/>
  </UserControl.Resources>

  <Grid>
    <StackPanel>
      <ComboBox Loaded="PostProcessComboBoxValues" Style="{DynamicResource EditorHComboBoxStyle}" IsEditable="False"
          ToolTip="{x:Static r:Definitions.MD_SecurityConstraints_classification}"
          ItemsSource="{Binding Source={StaticResource MD_ClassificationCode}, Path=List}"
          DisplayMemberPath="."
          v:Nav.AnchorName="ClasscationCd,ClasscationCd"
          SelectedValuePath="@value"
          Name="MD_ClassificationCode_Code">
        <ComboBox.SelectedValue>
          <e:MetadataBinding XPath="ClasscationCd/@value"/>
        </ComboBox.SelectedValue>
      </ComboBox>

      <!-- validate after all anchors are loaded -->
      <v:PageValidateControl/>

    </StackPanel>
  </Grid>
</p:EditorPage>
