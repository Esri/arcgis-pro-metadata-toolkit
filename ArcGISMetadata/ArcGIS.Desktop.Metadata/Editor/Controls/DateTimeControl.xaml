<UserControl Name="Control" x:Class="ArcGIS.Desktop.Metadata.Editor.Controls.DateTimeControl" 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:p="clr-namespace:ArcGIS.Desktop.Metadata.Editor.Pages"
    xmlns:e="clr-namespace:ArcGIS.Desktop.Metadata.Editor"
    xmlns:controls="clr-namespace:ArcGIS.Desktop.Metadata.Editor.Controls">

  <UserControl.Resources>
    <p:DateConverter x:Key="DateConverter" Format="yyyyMMdd"/>
    <p:VisibleEmptyConverter x:Key="VisibleEmptyConverter"/>
  </UserControl.Resources>

  <Grid>   
    <ListBox Style="{DynamicResource EditorNoScrollListBoxStyle}" ItemsSource="{Binding ElementName=Control, Path=Items}">
      <ListBox.ItemTemplate>
        <DataTemplate>
          <DockPanel LastChildFill="True">
            <Button Visibility="{Binding XPath=., Converter={StaticResource VisibleEmptyConverter}}" DockPanel.Dock="Right" Click="ClearItem" Style="{DynamicResource DeleteButton}">
              <Image Style="{DynamicResource DeleteImageStyle}" Source="pack://application:,,,/ArcGIS.Desktop.Resources;component/Images/GenericDeleteRed16.png" />
            </Button>
            <controls:DateTimePicker DisplayTime="False" SelectedDate="{e:MetadataBinding XPath=., Converter={StaticResource DateConverter}}"/>
          </DockPanel>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
  </Grid>
</UserControl>
